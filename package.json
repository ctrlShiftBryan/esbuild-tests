{
  "name": "esbuild-foo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": " esbuild main.ts --outfile=output.js --bundle --loader:.ts=ts",
    "build2": "esbuild App.js --bundle --outfile=AppBundle.js --loader:.js=jsx"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.47",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
